import React from 'react';
import { Input, Button } from '../../styled/';

const Search = props => {
    return (
        <div class="search-gem-container">
            <Input />
            <Button />
        </div>
    )
}

export default Search;