import React from 'react';
import { Search, List } from '../../common';

const Home = props => {
    return (
        <div class="home">
            <Search />
            <List />
        </div>
    )
}

export default Home;