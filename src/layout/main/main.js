import React from 'react';
import { Routes } from '../../routes';

const Main = (props) => {
    return (
        <div className="main">
            <Routes />
        </div>
    );
}

export default Main;